#ifndef _UTILS_H_
#define _UTILS_H_

#include <stdlib.h>

#define min(a,b) (((a)<(b))?(a):(b))
#define max(a,b) (((a)>(b))?(a):(b))

int RandomInt(int min, int max)
{
	return rand() % (max - min) + min;
}

#endif
